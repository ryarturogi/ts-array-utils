{
  "name": "ts-array-utils",
  "version": "1.0.0",
  "description": "multiple array utils, filters, shuffle, etc.",
  "main": "dist/index.js",
  "type": "commonjs",
  "types": "src/index.d.ts",
  "scripts": {
    "build": "babel src --out-dir dist --extensions \".ts\" --source-maps inline",
    "dev": "babel src --out-dir dist --extensions \".ts\" --source-maps inline",
    "dev:watch": "babel src --out-dir dist --extensions \".ts\" --source-maps inline --watch"
  },
  "keywords": [
    "array",
    "utils"
  ],
  "author": "Ricardo Guillen I",
  "license": "ISC",
  "dependencies": {
    "@babel/plugin-proposal-class-properties": "^7.18.6",
    "@babel/preset-env": "^7.20.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.20.7",
    "@babel/core": "^7.20.7",
    "@babel/preset-typescript": "^7.18.6"
  }
}
